# Windows 使用说明 🪟

## 批处理文件说明

本项目为Windows用户提供了多个批处理文件（.bat），方便不同场景下的使用。

### 🚀 推荐使用

#### 1. **一键安装并启动.bat** ⭐ 强烈推荐
- **适用场景**: 首次使用或快速启动
- **功能**: 自动检查环境 → 安装依赖 → 启动服务器 → 自动打开浏览器
- **优点**: 一步到位，最简单
```
双击运行即可
```

#### 2. **启动.bat**
- **适用场景**: 依赖已安装，正常启动
- **功能**: 完整的环境检查和启动流程
- **特点**: 详细的状态提示和错误处理

### 📦 安装相关

#### 3. **安装依赖.bat**
- **适用场景**: 单独安装或重新安装依赖
- **功能**: 
  - 检查Node.js和npm
  - 选择是否使用淘宝镜像
  - 清理旧依赖（可选）
  - 安装项目依赖
- **特点**: 提供交互式选择

#### 4. **使用淘宝镜像安装.bat**
- **适用场景**: 国内网络环境，下载速度慢
- **功能**: 
  - 自动配置淘宝镜像源
  - 清理旧依赖
  - 快速安装
- **优点**: 下载速度更快
- **注意**: 会修改npm全局配置

### ⚡ 快速操作

#### 5. **快速启动.bat**
- **适用场景**: 已经配置好，需要快速启动
- **功能**: 最小化输出，快速启动服务
- **特点**: 简洁，启动速度快

### 🔧 工具类

#### 6. **检查环境.bat**
- **适用场景**: 排查问题，检查配置
- **功能**: 全面检查
  - Node.js 安装状态和版本
  - npm 安装状态和版本
  - 项目依赖安装情况
  - 项目文件完整性
  - 端口占用情况
- **用途**: 问题诊断

#### 7. **停止服务器.bat**
- **适用场景**: 需要停止正在运行的服务器
- **功能**: 
  - 查找3000端口的进程
  - 显示进程信息
  - 安全停止进程
- **特点**: 交互式确认，防止误操作

#### 8. **启动菜单.bat** ⭐ 图形化菜单
- **适用场景**: 喜欢菜单式操作
- **功能**: 
  - 图形化菜单界面
  - 集成所有功能
  - 一键选择操作
- **特点**: 美观易用，适合新手

### 🎭 VBScript 静默启动

#### 9. **静默启动.vbs**
- **适用场景**: 不想看到命令窗口
- **功能**: 
  - 后台启动服务器
  - 自动打开浏览器
  - 首次运行自动安装依赖
- **特点**: 无黑色窗口，更美观

#### 10. **后台启动.vbs**
- **适用场景**: 完全静默启动
- **功能**: 
  - 纯后台运行
  - 自动打开浏览器
- **注意**: 需要手动安装依赖

#### 11. **停止服务.vbs**
- **适用场景**: 图形化停止服务
- **功能**: 
  - 对话框确认
  - 安全停止服务
- **特点**: 友好的图形界面

## 🎨 颜色说明

批处理文件使用不同颜色表示不同状态：

| 颜色 | 含义 | 说明 |
|------|------|------|
| 🟢 绿色 | 成功/正常 | 操作成功，一切正常 |
| 🔵 蓝色 | 信息/提示 | 一般信息提示 |
| 🟡 黄色 | 警告/处理中 | 需要注意或正在处理 |
| 🔴 红色 | 错误/失败 | 操作失败，需要处理 |

## 📝 使用流程

### 首次使用（推荐流程）

```
1. 双击 "一键安装并启动.bat"
   ↓
2. 等待自动安装依赖（2-5分钟）
   ↓
3. 浏览器自动打开 http://localhost:3000
   ↓
4. 开始使用！
```

### 完整流程（详细版）

```
1. 双击 "检查环境.bat" 检查系统环境
   ↓
2. 如果缺少依赖，运行 "安装依赖.bat"
   （国内用户可选择 "使用淘宝镜像安装.bat"）
   ↓
3. 双击 "启动.bat" 启动服务器
   ↓
4. 打开浏览器访问 http://localhost:3000
```

### 日常使用

```
方式1: 双击 "快速启动.bat" (最快)
方式2: 双击 "启动.bat" (查看详细信息)
方式3: 双击 "一键安装并启动.bat" (最稳妥)
```

## ❓ 常见问题

### Q1: 双击bat文件一闪而过，看不到内容？
**A**: 这是正常的，说明启动成功了。如果想看详细信息：
1. 右键点击.bat文件
2. 选择"编辑"
3. 查看代码最后是否有`pause`命令

或者：
1. 先打开命令提示符（Win+R，输入cmd）
2. 拖动.bat文件到命令提示符窗口
3. 按回车执行

### Q2: 提示"未检测到Node.js"？
**A**: 需要先安装Node.js
1. 访问 https://nodejs.org/
2. 下载LTS版本（推荐）
3. 安装完成后重启电脑
4. 重新运行bat文件

### Q3: 依赖安装很慢或失败？
**A**: 解决方法：
1. 使用 "使用淘宝镜像安装.bat"
2. 或者手动设置镜像：
   ```batch
   npm config set registry https://registry.npmmirror.com
   ```
3. 检查网络连接
4. 关闭代理或VPN

### Q4: 提示"端口3000已被占用"？
**A**: 解决方法：
1. 运行 "停止服务器.bat"
2. 或者手动查找并结束进程：
   ```batch
   netstat -ano | findstr "3000"
   taskkill /PID [进程ID] /F
   ```

### Q5: 中文显示乱码？
**A**: bat文件已设置UTF-8编码（chcp 65001）
如果仍然乱码：
1. 右键点击命令提示符标题栏
2. 选择"属性"
3. 在"选项"中选择"使用旧版控制台"
4. 重新运行

### Q6: 提示权限不足？
**A**: 以管理员身份运行：
1. 右键点击.bat文件
2. 选择"以管理员身份运行"

### Q7: 想卸载/清理项目？
**A**: 
1. 删除 `node_modules` 文件夹
2. 删除 `package-lock.json`
3. 删除 `config.json` 和 `saves` 文件夹（如果要清除数据）

### Q8: 如何更新依赖？
**A**: 
1. 运行 "安装依赖.bat"
2. 选择重新安装
3. 或手动执行：
   ```batch
   rmdir /s /q node_modules
   del package-lock.json
   npm install
   ```

## 🔧 高级技巧

### 1. 修改服务器端口
编辑 `server.js`，修改：
```javascript
const PORT = process.env.PORT || 3000;
```
改为：
```javascript
const PORT = process.env.PORT || 你想要的端口;
```

### 2. 设置环境变量
创建 `.env` 文件：
```
PORT=8080
```

### 3. 创建桌面快捷方式
1. 右键拖动.bat文件到桌面
2. 选择"在当前位置创建快捷方式"
3. 可以修改图标和名称

### 4. 开机自启动
1. Win+R 输入 `shell:startup`
2. 将.bat文件快捷方式复制到打开的文件夹

### 5. 后台运行
使用VBS脚本隐藏窗口：
```vbscript
CreateObject("Wscript.Shell").Run "启动.bat", 0, False
```
保存为 `后台启动.vbs`

## 📊 批处理文件对比

| 功能 | 一键安装并启动 | 启动 | 快速启动 | 安装依赖 | 检查环境 |
|------|---------------|------|---------|---------|---------|
| 环境检查 | ✓ | ✓ | 简化 | ✓ | ✓✓ |
| 自动安装 | ✓ | ✓ | ✓ | ✓ | ✗ |
| 启动服务 | ✓ | ✓ | ✓ | ✗ | ✗ |
| 打开浏览器 | ✓ | ✗ | ✗ | ✗ | ✗ |
| 详细输出 | ✓ | ✓ | ✗ | ✓ | ✓✓ |
| 交互选项 | ✗ | ✗ | ✗ | ✓ | ✗ |
| 适合新手 | ⭐⭐⭐ | ⭐⭐ | ⭐ | ⭐⭐ | ⭐⭐⭐ |

## 💡 使用建议

### 新手用户
1. 直接使用 **"一键安装并启动.bat"**
2. 遇到问题运行 **"检查环境.bat"**

### 开发者
1. 首次使用 **"安装依赖.bat"** 查看详细过程
2. 日常使用 **"快速启动.bat"** 节省时间
3. 调试时使用 **"启动.bat"** 查看详细信息

### 国内用户
1. 优先使用 **"使用淘宝镜像安装.bat"**
2. 大幅提升下载速度

## 🎯 最佳实践

1. **首次运行**: 使用"检查环境.bat"确保环境正确
2. **安装依赖**: 国内用户优先使用淘宝镜像
3. **日常使用**: 使用"快速启动.bat"最方便
4. **遇到问题**: 先运行"检查环境.bat"诊断
5. **完全关闭**: 使用"停止服务器.bat"而不是直接关窗口

## 📞 获取帮助

如果遇到问题：
1. 运行 "检查环境.bat" 查看详细状态
2. 查看命令提示符中的错误信息
3. 检查 README.md 和 USAGE.md
4. 查看项目GitHub Issues

---

**祝你使用愉快！** 🎉

更多详细信息请参考：
- `README.md` - 项目说明
- `USAGE.md` - 详细使用指南
- `快速开始.md` - 快速入门教程
